# myLinter — линтер для проверки лог-записей в Go

`myLinter` — это кастомный линтер для Go, совместимый с `golangci-lint`, который проверяет лог-сообщения в коде на соответствие установленным правилам.

---

## Требования

- Go 1.22+
- golangci-lint v2.x
- Поддерживаемые логгеры:
  - `log/slog`
  - `go.uber.org/zap`

---

## Установка

1. Склонируйте репозиторий:

```bash
git clone https://github.com/dkar-dev/mylinter.git
cd mylinter

    Соберите бинарник линтера:

go build -o mylinter.exe ./cmd/mylinter

    Убедитесь, что golangci-lint версии 2.x установлена:

golangci-lint --version
# Должно вывести что-то вроде v2.x.x
```
## Использование
1. Прямой запуск линтера

Можно тестировать линтер напрямую:

`go test ./...`
##### или
`go run ./cmd/mylinter ./...`

2. Интеграция с golangci-lint v2

    В корне проекта создайте .golangci.yml:
```yml
version: 2

linters-settings:
  mylinter:
    path: ./mylinter.exe
    type: external

linters:
  enable:
    - mylinter
```
    
Запустите golangci-lint:

`golangci-lint run`

  Важно: golangci-lint должен быть версии 2.x, иначе внешний линтер не будет работать.

✅ Правила проверки логов

  - Сообщение должно начинаться со строчной буквы:
```
log.Info("starting server") // правильно
log.Info("Starting server") // ❌ неправильно
```
  - Логи только на английском:
```
log.Info("server started") // правильно
log.Info("сервер запущен") // ❌ неправильно
```
  - Не использовать спецсимволы или эмодзи:
```
log.Info("server started")   // правильно
log.Info("server started!")  // ❌ неправильно
log.Info("server 🚀")        // ❌ неправильно
```
  - Не включать чувствительные данные:
```
log.Info("user authenticated successfully") // правильно
log.Info("user password: " + password)     // ❌ неправильно
```
📂 Примеры использования

Пример кода для тестирования линтера:
```
package main

import (
    "log"
)

func main() {
    slog.Println("starting server")   // ✅ пройдет проверку
	  slog.Info("Starting server")    // want "log message should start with a lowercase letter"
                                    // указываем ожидаемое поведение - ✅ тоже пройдет проверку
}
```
Запуск линтера:
```
go run ./cmd/mylinter ./...
# или через golangci-lint
golangci-lint run
```
